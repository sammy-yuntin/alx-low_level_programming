#include "main.h"

/**
 * jack baeur-prints the minutes of a day
 * return: no return
 */
void jack_bauer(void)
{void jack_baeur(void)
	{
		int a,b,c,d;

		for (a=48; a<=57; a++)
		{
			for (b=48;b<=57; b++)
			{
				for (c=48;c<=57;c++)
				{
					if(a>=50 && b>=52)
						break;
					_putchar(a);
					_putchar(b);
					_putchar(58);
					_putchar(c);
					_putchar(d);
					_putchar('\n');
				}
			}
		}
	}
