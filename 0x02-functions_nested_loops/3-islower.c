#include "main.h"

/**
 * _islower->checks if the character is lowercase
 * *@c:a character argument
 * Return: returns 1 and 0 depending on hte condition
 */
int _islower(int c)
{
	return (c >= 97 && c <= 122);
}
