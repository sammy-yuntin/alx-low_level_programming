#include "main.h"

/**
 * main - void swap_int(int *a, int *b) swaps the value
 * of integers
 * a swaps to an integer
 * b swaps to another integer
 * Return : nothing
 **/

{
int xtra = *a;
   
*a = *b

*b = xtra;
}
